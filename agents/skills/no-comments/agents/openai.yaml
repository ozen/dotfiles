interface:
  display_name: "No Comments"
  short_description: "Review comments and fix accepted findings"
  default_prompt: "Use $no-comments to review the comments in this change and fix accepted findings."

policy:
  allow_implicit_invocation: false
