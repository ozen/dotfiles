interface:
  display_name: "Encode Lessons in Structure"
  short_description: "Encode repeated lessons in checks and tooling"
  default_prompt: "Use $principle-encode-lessons-in-structure to encode this recurring lesson in structure or tooling."

policy:
  allow_implicit_invocation: false
