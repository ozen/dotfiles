interface:
  display_name: "Make Operations Idempotent"
  short_description: "Design operations that converge safely on retries"
  default_prompt: "Use $principle-make-operations-idempotent to make this operation converge safely across retries."

policy:
  allow_implicit_invocation: false
