interface:
  display_name: "Recall"
  short_description: "Reconstruct recent work into a current-state brief"
  default_prompt: "Use $recall to reconstruct my recent work and give me a current-state brief."

policy:
  allow_implicit_invocation: false
