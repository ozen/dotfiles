interface:
  display_name: "Show Me Your Work"
  short_description: "Keep an auditable decision trail for long-running work"
  default_prompt: "Use $show-me-your-work to keep an auditable decision trail for this work."

policy:
  allow_implicit_invocation: false
