interface:
  display_name: "Unslop"
  short_description: "Remove AI writing tells and empty phrasing"
  default_prompt: "Use $unslop to remove AI writing tells and empty phrasing from this text."

policy:
  allow_implicit_invocation: false
